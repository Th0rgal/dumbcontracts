---
title: Verity
description: A Lean-native language for verified smart contracts
---

import { Callout } from 'nextra/components'
import { VerityHero } from '../components/VerityHero'
import { CodeCompare } from '../components/CodeCompare'
import { ReportCallout } from '../components/ReportCallout'

<VerityHero />

<CodeCompare />

<ReportCallout title="Current verification posture">
Verity is a compact Lean EDSL with verified example contracts, automatic compilation
to EVM artifacts, and explicit proof boundaries for the compiler model. The docs are
written as a manual: start with a contract, inspect the compiler model, then review
the theorem and assumption inventory. Layer 2 proof status is tracked in
`Compiler/Proofs/IRGeneration/Contract.lean` (generic whole-contract
`CompilationModel -> IR` theorem for the current supported fragment).
</ReportCallout>

## The Three-Layer Structure

Each contract is written as a specification, an executable Lean implementation, and
a proof that connects the two.

### Specification

```verity
def mint_spec (to : Address) (amount : Uint256) (s s' : ContractState) : Prop :=
  s'.storageMap 1 to = add (s.storageMap 1 to) amount ∧
  s'.storage 2 = add (s.storage 2) amount ∧
  storageMapUnchangedExceptKeyAtSlot 1 to s s' ∧
  sameContext s s'
```

### Implementation

```verity
def mint (to : Address) (amount : Uint256) : Contract Unit := do
  onlyOwner
  let currentBalance ← getMapping balances to
  let newBalance ← requireSomeUint (safeAdd currentBalance amount) "Balance overflow"
  let currentSupply ← getStorage totalSupply
  let newSupply ← requireSomeUint (safeAdd currentSupply amount) "Supply overflow"
  setMapping balances to newBalance
  setStorage totalSupply newSupply
```

### Proof

```verity
theorem mint_meets_spec (s : ContractState) (to : Address) (amount : Uint256)
  (h_owner : s.sender = s.storageAddr 0) :
  let s' := ((mint to amount).run s).snd
  mint_spec to amount s s' := by
  simp only [mint, onlyOwner, getMapping, setMapping]
  simp [h_owner]
```

The Lean compiler verifies every proof. If a proof is wrong, compilation fails.

## What the Manual Covers

| Section | Use it for |
|---|---|
| [Getting Started](/getting-started) | Local setup, Lean prerequisites, and first verification run |
| [First Contract](/guides/first-contract) | A guided spec-to-proof walkthrough |
| [Compiler](/compiler) | The EDSL-to-EVM pipeline, trust model, and reports |
| [Verification](/verification) | Current theorem inventory and proof status |
| [Solidity to Verity](/guides/solidity-to-verity) | Translation patterns for Solidity contracts |
| [Production Solidity Patterns](/guides/production-solidity-patterns) | Modifiers, linked externals, ABI projection, events, and oracle boundaries |

<Callout type="info">
**For agents and tooling:** check [/llms.txt](/llms.txt) or add `.md` to any page
URL to retrieve raw markdown.
</Callout>
